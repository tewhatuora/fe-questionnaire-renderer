/// <reference types="react" />
import { Input } from '../../../types';
interface CheckboxInputProps {
    input: Input;
}
export default function CheckboxInput({ input }: CheckboxInputProps): JSX.Element;
export {};

/// <reference types="react" />
import { Input } from '../../../types';
interface Props {
    input: Input;
}
export default function ItemRepeatButtons({ input }: Props): JSX.Element | null;
export {};
